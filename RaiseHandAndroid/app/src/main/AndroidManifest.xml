<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.sae1.raisehand">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <!-- <uses-permission android:name="android.permission.READ_CONTACTS" /> -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="app.MainActivity"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="utils.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="app.TeacherClasses"/>
        <activity android:name="app.TeacherHomePage"/>
        <activity android:name="app.TeacherNotifications"/>
        <activity android:name="app.TeacherSettings"/>
        <activity android:name="app.TeacherStudents"/>
        <activity android:name="app.TeacherQuestions"/>
        <activity android:name="app.TeacherTopics"/>
        <activity android:name="utils.SignupActivity"/>
        <activity android:name=".student_notifications"/>
        <activity android:name=".student_settings"/>
        <activity android:name=".student_classes"/>
        <activity android:name=".student_home_page"/>
        <!-- <activity android:name="utils.QuestionActivity"></activity> -->
        <activity android:name=".student_topics"/>
        <activity android:name=".student_questions"/>
        <activity android:name="app.MakeQuestion">
        </activity>
        <activity android:name="app.TeacherReplies">
        </activity>
    </application>

</manifest>